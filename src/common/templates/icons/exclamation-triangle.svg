<svg class="{{ class_attr }}" viewBox="0 0 24 24" fill="currentColor" {{ accessibility_attrs }}>
  <!-- Triangle outline -->
  <path fill-rule="evenodd" d="M12 2L1 21h22L12 2zm0 3.5L20.5 19h-17L12 5.5z"/>
  <!-- Exclamation stem -->
  <rect x="11" y="10" width="2" height="4" rx="0.5"/>
  <!-- Exclamation dot -->
  <circle cx="12" cy="17" r="1.25"/>
</svg>